{
  "name": "meet-scheduler",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter api prisma migrate dev",
    "db:generate": "pnpm --filter api prisma generate",
    "db:studio": "pnpm --filter api prisma studio",
    "gojiberry:db:migrate": "pnpm --filter gojiberry-api prisma migrate dev",
    "gojiberry:db:generate": "pnpm --filter gojiberry-api prisma generate",
    "gojiberry:db:studio": "pnpm --filter gojiberry-api prisma studio"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "msgpackr-extract",
      "@scarf/scarf"
    ]
  }
}
